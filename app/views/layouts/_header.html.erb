<header class="bg-warning pb-2 pt-2">
	<div class="container">
    <div class="row">
    	<div class="col-4">
    	  <%= link_to root_path do %><h5 class="text-white"><i class="far fa-comments header-icon"></i> Chat app</h5><% end %>
      </div>
      <div class="col-4"></div>
 <!-- ---------------ログイン時--------------------- -->
      <div class="col-4">
  	  <% if user_signed_in? %>
        <%= link_to posts_path do %>
          <div class="btn btn-outline-light">Timeline</div>
        <% end %>
        <%= link_to user_path(current_user) do %>
          <div class="btn btn-outline-light">My page</div>
  	    <% end %>
  	    <%= link_to users_path do %>
          <div class="btn btn-outline-light">Users</div>
  	    <% end %>
  	    <%= link_to destroy_user_session_path, method: :delete  do %>
  	      <div class="btn btn-outline-light">Log out</div>
  	    <% end %>
<!-- ----------------------非ログイン時--------------- -->
      <% end %>
      </div>
    </div>
  </div>
</header>